# Backend MCP Server

A simple MCP server built with FastMCP v3.

## Setup

```bash
uv sync --prerelease=allow
```

## Run

```bash
uv run python main.py
```

Server starts at `http://localhost:8000/mcp`.
